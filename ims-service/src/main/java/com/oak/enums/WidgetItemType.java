package com.oak.enums;

// NOTE: This is currently used for Sorting but could be extended
public enum WidgetItemType {
	ASSET_TYPE_ID, ASSET_NAME, READING_DATE, END_DATE, ELEMENT_ID, SOURCE_NAME;
}
